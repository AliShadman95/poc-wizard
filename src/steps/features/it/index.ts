/**
 * What a market folder exposes. Every market's index has this same shape, so adding one
 * means copying a folder rather than remembering which files to create.
 */
export { Sections } from './Sections';
export { featuresInitialValues, featuresSchema } from './schema';
